# 2026-06-17 작업 로그

## 11:30 - [문서 셋팅]

### 요청 내용
사내 브랜치/버전/커밋 표준 + 설계 원칙·패턴·컨벤션 문서 두 개를
통합해 프로젝트 루트 CLAUDE.md 생성.

### 수행한 작업
- 기존 CLAUDE.md 존재 확인 → 없음 → 신규 생성
- 통합 구조:
  - 사내 표준(브랜치/버전/커밋) → `[REQUIRED]` 분류 상단 배치
  - 설계 원칙/패턴 17개 섹션 → 하단 별도 섹션 유지
- 생성 파일: /factory/keepalived-tools/CLAUDE.md (712줄)
- 전역 가이드 상속, 충돌 시 본 문서 우선 명시

### 결과
성공. CLAUDE.md 생성 완료.

## 12:03 - [사전 조사]

### 요청 내용
keepalived VSCode 익스텐션 기획용 사전 조사 3건:
설정 파일 구조, 기존 익스텐션 유무, 익스텐션 기술 스택.

### 수행한 작업
- 웹 검색/페치: keepalived.conf man page(Ubuntu focal),
  VSCode 마켓플레이스 검색, vsce 배포 절차, TextMate vs LSP
- 마켓플레이스 gallery API 직접 쿼리(filterType 10 "keepalived")
  → COUNT 0 확인
- 블록 계층/지시어/값 타입/주석 문법 정리

### 결과
성공. 조사 보고 완료. 핵심:
- keepalived 전용 VSCode 익스텐션 사실상 부재(마켓 API 0건)
- 하이라이팅은 무코드(tmLanguage), 검증은 LSP/Diagnostics 필요
- 주석은 # 또는 ! 둘 다. 블록은 {} 중첩.

## 13:00 - [오픈소스 활용 조사]

### 요청 내용
keepalived 오픈소스를 익스텐션에 최대 활용하는 방안 조사.
소스 키워드 추출 가능성, config-test 존재, 라이선스.

### 수행한 작업
- keepalived master clone 후 소스 직접 확인
- install_keyword 등록 구조, parser.h keyword_t, config_err_t enum 확인
- --config-test(-t) 존재 + 줄번호 출력 확인
- 라이선스 GPL-2.0-or-later 확인

### 결과
성공. 핵심:
- 지시어·중첩 = install_keyword 정적 파싱으로 자동추출 가능
- 값 타입 = 핸들러 로직 안 → 반자동(manpage 보강)
- config-test = 설치 의존 → 미사용 결정
- 사실 데이터 추출은 저작권 비대상(Feist)

## 13:30 - [기획·설계 문서화]

### 요청 내용
미합의 기능까지 논의 확정 후 docs/ 계층으로 설계 문서 작성.
프로젝트 방향: keepalived 최대 활용. 작업로그+다음세션 인계.

### 수행한 작업
- 기획 결정 확정 (대화):
  - 라이선스 GPL-2.0-or-later, keepalived 소스 최대 활용
  - TypeScript 단일, LSP 미채택(core/vscode 분리로 확장여지)
  - 검증 4층 풀스펙(구문+타입+의미+include)
  - 하이라이팅 고급문법 전부, 스니펫+포맷터 포함
  - 풀기능 v1.0 일괄 공개
- 브랜치 docs-design 생성 (main 분기)
- 설계 문서 8개 작성:
  - docs/00-planning/{00-features,01-tech-decisions,02-license,03-roadmap}.md
  - docs/01-architecture/{00-overview,01-schema,02-parser,03-validation}.md
- tasks/todo.md 인계 체크리스트 작성

### 결과
성공. 설계 문서화 완료. 코드는 아직 0줄.
다음: 문서 커밋 → S0 프로젝트 셋업 → S1 스키마 추출.
※ 미커밋 상태. 첫 릴리스면 main에서 v0.1.0 생성 후 MR 필요.
